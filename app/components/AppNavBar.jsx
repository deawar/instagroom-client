import React from 'react';
import {

} from 'react-router-native';
import {
    StyleSheet,
    Text,
} from 'react-native';

function Navbar() {
    return (
        <Text>NavBar</Text>
    );
}

const styles = StyleSheet.create({

})

export default Navbar;
